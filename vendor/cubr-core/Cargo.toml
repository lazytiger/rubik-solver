# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "cubr-core"
version = "0.2.0"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Pure, Bevy-free 3x3x3 Rubik's cube model (integer-math CubeCore) and a guaranteed-optimal Korf solver with a two-phase fallback"
readme = "README.md"
keywords = [
    "rubiks-cube",
    "cube",
    "solver",
    "korf",
    "puzzle",
]
categories = [
    "algorithms",
    "mathematics",
]
license = "MIT"
repository = "https://github.com/buzzlightyear1309/cubr"

[lib]
name = "cubr_core"
path = "src/lib.rs"

[[test]]
name = "bench_solve"
path = "tests/bench_solve.rs"

[dependencies.glam]
version = "0.30"

[dependencies.serde]
version = "1"
features = ["derive"]

[dev-dependencies.kewb]
version = "0.4"

[dev-dependencies.serde_json]
version = "1"

# ── 本项目补丁(wasm)──
# wasm32-unknown-unknown 上 std 的 Instant/SystemTime 是 unimplemented!(),
# 而两阶段求解器内部用了 Instant 做截止时间判断。web-time 在浏览器里走
# performance.now(),原生平台直接转发给 std,两边行为一致。
[dependencies.web-time]
version = "1"
